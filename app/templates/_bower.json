{
  "name": "<%= _.camelize(appname) %>",
  "version": "0.0.0",
  "private": true,
  "dependencies": {
    <% if (bootstrap) { %>"bootstrap-sass": "~3.0.0",
    <% } %>"trabian-webapp-core": "~0.0.4"
  }
}
